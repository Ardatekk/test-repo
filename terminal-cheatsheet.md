# 🐧 Terminal Mini Cheat Sheet

## 📂 Navigation
- `pwd` → shows current folder
- `ls` → list files
- `cd folder/` → enter folder
- `cd ..` → go back

## 📦 Package Manager (apt)
- `sudo apt update` → update package list
- `sudo apt upgrade` → upgrade packages
- `apt search name` → search package
- `sudo apt install package` → install package
- `sudo apt remove package` → remove package
- `sudo apt autoremove` → clean unnecessary packages

## 🔑 Permissions
- `ls -l` → show permissions
- `chmod u+rwx file` → give all rights to owner
- `chmod g+rwx file` → give all rights to group
- `chmod o-rwx file` → remove rights from others

## 🧠 Useful Tricks
- `man command` → open manual
- `command --help` → quick help
- `curl cheat.sh/ls` → online cheat sheet

## 🌟 Git Basics
- `git status`
- `git add .`
- `git commit -m "message"`
- `git push`
